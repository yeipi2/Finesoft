@* fs-front/Components/Clients/Shared/ClientStatusBadge.razor *@

<span class="badge @(IsActive ? "bg-success" : "bg-secondary")">
    @(IsActive ? "Activo" : "Inactivo")
</span>

@code {
    [Parameter] public bool IsActive { get; set; }
}