@page "/Pending"
@layout AuthLayout

<PageTitle>Cuenta pendiente - Finesoft</PageTitle>

<div class="text-center">
    <h1 class="h5 fw-semibold">Tu cuenta está pendiente ⏳</h1>
    <p class="text-muted small">
        Tu registro fue exitoso, pero todavía necesitas que un administrador te asigne un rol
        para poder acceder al sistema.
    </p>

    <div class="alert alert-info small mt-3" role="alert">
        Cuando un administrador te asigne permisos, cierra sesión e inicia sesión nuevamente.
    </div>

    <button class="btn btn-outline-primary mt-2" @onclick="Logout">
        Cerrar sesión
    </button>
</div>

@inject AuthenticationStateProvider Provider
@inject NavigationManager Nav

@code {
    private void Logout()
    {
        var auth = (fs_front.Services.CustomAuthStateProvider)Provider;
        auth.Logout();
        Nav.NavigateTo("/iniciar-sesion", true);
    }
}