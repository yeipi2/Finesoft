<section class="features">
    <div class="container">
        <Animate Animation="Animations.FadeUp" Duration="TimeSpan.FromSeconds(0.8)">
            <div class="section-header">
                <h2>Todo lo que necesitas</h2>
                <p>Una plataforma completa para gestionar tu negocio</p>
            </div>
        </Animate>

        <div class="features-grid">
            <Animate Animation="Animations.FadeUp" Duration="TimeSpan.FromSeconds(0.8)" Delay="TimeSpan.FromSeconds(0.1)">
                <div class="feature-card">
                    <div class="feature-icon purple">
                        <i class="bi bi-people"></i>
                    </div>
                    <h3>Gestión de Clientes</h3>
                    <p>Administra tu cartera completa con información detallada y organizada.</p>
                </div>
            </Animate>

            <Animate Animation="Animations.FadeUp" Duration="TimeSpan.FromSeconds(0.8)" Delay="TimeSpan.FromSeconds(0.2)">
                <div class="feature-card">
                    <div class="feature-icon blue">
                        <i class="bi bi-kanban"></i>
                    </div>
                    <h3>Proyectos</h3>
                    <p>Control total del avance, tareas y entregables de cada proyecto.</p>
                </div>
            </Animate>

            <Animate Animation="Animations.FadeUp" Duration="TimeSpan.FromSeconds(0.8)" Delay="TimeSpan.FromSeconds(0.3)">
                <div class="feature-card">
                    <div class="feature-icon orange">
                        <i class="bi bi-ticket-perforated"></i>
                    </div>
                    <h3>Sistema de Tickets</h3>
                    <p>Gestión completa de soporte técnico e incidencias.</p>
                </div>
            </Animate>

            <Animate Animation="Animations.FadeUp" Duration="TimeSpan.FromSeconds(0.8)" Delay="TimeSpan.FromSeconds(0.4)">
                <div class="feature-card">
                    <div class="feature-icon green">
                        <i class="bi bi-receipt"></i>
                    </div>
                    <h3>Facturación</h3>
                    <p>Crea facturas, cotizaciones y lleva control financiero completo.</p>
                </div>
            </Animate>

            <Animate Animation="Animations.FadeUp" Duration="TimeSpan.FromSeconds(0.8)" Delay="TimeSpan.FromSeconds(0.5)">
                <div class="feature-card">
                    <div class="feature-icon pink">
                        <i class="bi bi-graph-up"></i>
                    </div>
                    <h3>Reportes</h3>
                    <p>Análisis detallados y métricas para tomar mejores decisiones.</p>
                </div>
            </Animate>

            <Animate Animation="Animations.FadeUp" Duration="TimeSpan.FromSeconds(0.8)" Delay="TimeSpan.FromSeconds(0.6)">
                <div class="feature-card">
                    <div class="feature-icon teal">
                        <i class="bi bi-shield-check"></i>
                    </div>
                    <h3>Seguridad</h3>
                    <p>Sistema de permisos granular y autenticación robusta.</p>
                </div>
            </Animate>
        </div>
    </div>
</section>
